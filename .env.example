# Notion integration token — create at https://www.notion.so/profile/integrations
# The integration must be connected to the root Wilshire page (Share menu → Connections).
NOTION_TOKEN=ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# HTTP Basic Auth password for the dashboard (username is always "wilshire").
# Pick something long and random; share with sponsors via 1Password or encrypted channel.
DASHBOARD_PASSWORD=change-me-before-deploy
